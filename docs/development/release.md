# Release

Not yet defined - no releases exist. Will cover: version domains (spec
section 15), packaging smoke tests, old-fixture project migration checks, and
changelog cut. Main/release CI gates beyond PR gates: E2E smoke, packaging
smoke, clean project create/open, docs link check, generated schema/docs
drift check.
